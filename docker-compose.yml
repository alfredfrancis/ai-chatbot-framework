version: '2'
services:
  web:
    build: .
    ports:
        - "8080:8080"
    environment:
      APPLICATION_ENV: Production
    links:
      - mongodb
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    hostname: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodbdata:/data

volumes:
  mongodbdata:
